# Local development override for building Odoo from source
# Usage: docker compose -f e2e/docker-compose.yml -f e2e/docker-compose.local.yml up

services:
  odoo:
    # Override to build locally instead of pulling from GHCR
    build:
      context: ../odoo
      dockerfile: Dockerfile
    image: safee-odoo:local
