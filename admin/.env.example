# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:3000

# Better Auth (shared with gateway)
BETTER_AUTH_URL=http://admin.localhost:8080/api/v1
BETTER_AUTH_SECRET=your-secret-here

# Database (for direct queries)
DATABASE_URL=postgresql://safee:safee@localhost:15432/safee

# Redis (for monitoring)
REDIS_URL=redis://localhost:16379

# Cookie Domain (for session sharing across subdomains)
COOKIE_DOMAIN=.localhost

# Environment
NODE_ENV=development
